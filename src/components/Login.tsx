import React, { FunctionComponent, useState } from 'react';

const Login:FunctionComponent = () => {
    const user = useState('');

    return (
      <div>Login Page</div>
    )
}

export default Login;